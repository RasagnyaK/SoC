#include <iostream>
using namespace std;

int main() {
    long long a,b,c,d;
    cin >> a >> b >> c >> d;
    if (a != d) {
        cout << 0;
        return 0;
    }

    if (c > 0 && a == 0) {
        cout << 0;
        return 0;
    }

    cout << 1;
    return 0;
}
